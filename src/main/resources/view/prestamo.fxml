<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.biblioteca.controller.PrestamoController">

    <top>
        <VBox spacing="15" style="-fx-background-color: white; -fx-padding: 20;">
            <Label text="🔄 Gestión de Préstamos" style="-fx-text-fill: #2c3e50;">
                <font>
                    <Font name="System Bold" size="24"/>
                </font>
            </Label>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <VBox spacing="5">
                    <Label text="Usuario:" style="-fx-font-weight: bold;"/>
                    <ComboBox fx:id="cmbUsuario" promptText="Seleccionar Usuario" prefWidth="200"/>
                </VBox>

                <VBox spacing="5">
                    <Label text="Ejemplar (Libro):" style="-fx-font-weight: bold;"/>
                    <ComboBox fx:id="cmbEjemplar" promptText="Seleccionar Libro Disponible" prefWidth="250"/>
                </VBox>

                <VBox spacing="5">
                    <Label text="Días:" style="-fx-font-weight: bold;"/>
                    <TextField fx:id="txtDias" text="14" prefWidth="60" disable="true"/>
                </VBox>

                <Button text="➕ Prestar" onAction="#handleRealizarPrestamo"
                        style="-fx-background-color: #2ecc71; -fx-text-fill: white; -fx-cursor: hand;">
                    <VBox.margin>
                        <Insets top="20"/>
                    </VBox.margin>
                </Button>

                <Region HBox.hgrow="ALWAYS"/>

                <Button text="🔄 Actualizar" onAction="#handleActualizar"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-cursor: hand;">
                    <VBox.margin>
                        <Insets top="20"/>
                    </VBox.margin>
                </Button>
            </HBox>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Label text="Ver:" style="-fx-font-weight: bold;"/>
                <ComboBox fx:id="cmbFiltroEstado" prefWidth="150"/>
                <Button text="Filtrar" onAction="#handleFiltrar"
                        style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <VBox style="-fx-padding: 20;">
            <TableView fx:id="tablaPrestamos" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                    <TableColumn fx:id="colUsuario" text="Usuario" prefWidth="200"/>
                    <TableColumn fx:id="colLibro" text="Libro" prefWidth="200"/>
                    <TableColumn fx:id="colCodigoBarras" text="Código" prefWidth="120"/>
                    <TableColumn fx:id="colFechaSalida" text="Salida" prefWidth="120"/>
                    <TableColumn fx:id="colFechaEsperada" text="Vencimiento" prefWidth="120"/>
                    <TableColumn fx:id="colEstado" text="Estado" prefWidth="100"/>
                </columns>
            </TableView>

            <HBox spacing="10" alignment="CENTER_RIGHT" style="-fx-padding: 15 0 0 0;">
                <Button text="✅ Recibir Devolución" onAction="#handleDevolver"
                        style="-fx-background-color: #f39c12; -fx-text-fill: white; -fx-padding: 10 20; -fx-cursor: hand;"/>
                <Button text="👁️ Detalles" onAction="#handleVerDetalles"
                        style="-fx-background-color: #9b59b6; -fx-text-fill: white; -fx-padding: 10 20; -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </center>
</BorderPane>